function add(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first+second;
    myform.result.value=result;
}
 
function subtract() {
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first-second;
    myform.result.value=result;
}
 
function multiply(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first*second;
    myform.result.value=result;
}

function divide(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first/second;
    myform.result.value=result;
}